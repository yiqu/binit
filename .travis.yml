language: node_js
node_js:
  - "9"
  - "10"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - echo "Building for PROD"
  - ng build --prod --base-href=/binit/

after_success:
  - echo "Build SUCCESS!"

